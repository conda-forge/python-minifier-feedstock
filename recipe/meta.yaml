{% set name = "python-minifier" %}
{% set version = "2.11.1" %}


package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/python_minifier-{{ version }}.tar.gz
  sha256: f9c4b45ee814b65da6b58cc44772e0ef481d3ae4d1aac3e7c7b41740c3678f7f

build:
  number: 0
  noarch: python
  entry_points:
    - pyminify=python_minifier.__main__:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - pip
    - python >=3.3,<3.13
    - setuptools
  run:
    - python >=3.3,<3.13

test:
  imports:
    - python_minifier
  commands:
    - pip check
    - pyminify --help
  requires:
    - pip

about:
  home: https://github.com/dflook/python-minifier
  summary: Transform Python source code into it's most compact representation
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - thewchan
